apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ openshift_pg_pvc_name }}
  namespace: {{ openshift_project }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
